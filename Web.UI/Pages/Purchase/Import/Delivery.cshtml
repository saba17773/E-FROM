@page
@model Web.UI.Pages.Purchase.Import.DeliveryModel

@using Infrastructure.Models

@{
    ViewData["Title"] = "KPI Delivery";

    ViewData["Breadcrumb"] = new List<BreadcrumbModel> {
        new BreadcrumbModel { Text = "Import", Link = "javascript:void();" },
        new BreadcrumbModel { Text = "Import KPI Delivery" }
    };
}

<div class="card">
    <div class="card-header">
        KPI Delivery
    </div>
    <div class="card-body">
        <form method="post" enctype="multipart/form-data">
            <input type="file" name="TemplateUpload" />

            <button type="submit" class="btn btn-success">Submit</button>
        </form>
    </div>
</div>
